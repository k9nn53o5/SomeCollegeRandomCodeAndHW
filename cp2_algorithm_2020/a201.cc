#include <iostream>
#include <cstdio>
#include <algorithm>

bool myfunc(int i,int j){
	return (i<j);
}

int main(){
	int n;
	cin>>n;
	for(int i=0;i<n;i++){
		vector <int> vec;
		for(){
			int tmp;
			cin>>tmp;
			vec.push.push(tmp);
		}
	}
	return 0;
}
