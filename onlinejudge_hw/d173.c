#include<stdio.h>
#include<stdlib.h>

int main(){
int floor[];
int popularlation[];
int i=0;
while(scanf(" %d %d",&floor[i],&population[i]) != EOF){
	if(floor == 0 && population == 0){

		printf("%d %d",stopfloor,sumfloor);
		i=0;
	}
	i++;
}
return 0;
}
